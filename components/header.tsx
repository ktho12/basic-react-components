import React from "react";

const Header = () => {
    return (
        <header style={{
            border: '1px solid white', padding: '10px', textAlign: 'center'}}>
            <h1>Welcome to my introductory React App</h1>
        </header>
    );
};

export default Header;